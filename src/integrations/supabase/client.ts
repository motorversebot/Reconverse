// Supabase client REMOVED — Reconverse now uses Genesis Mission Control via API proxy.
// This stub exists only to prevent import errors during migration.
// All data access goes through @/lib/api → /api/v1/reconverse/... → gateway.tdreman.app/mc

export const supabase = null as any;
